{
  "name": "socket-api",
  "version": "1.0.0",
  "description": "socket.io client and c# server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io-client": "^1.4.5"
  }
}
